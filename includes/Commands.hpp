#ifndef COMMANDS_HPP
#define COMMANDS_HPP

enum CommandList {
	JOIN,
	LEAVE,
	QUIT,
	NICK,
	LIST,
	TOPIC_USER,
	TOPIC_OPER,
	MODE_USER,
	MODE_OPER,
	KICK,
	INVITE,
};

class Commands {
   public:
   private:
};

#endif